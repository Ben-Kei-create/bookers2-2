<div class="container">
<div class="row mt-4">
<div class="mx-auto">
<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <h3><%= @book.errors.count %> errors prohibited this obj from being saved:</h3>
  <ul>
   <% @book.errors.full_messages.each do |message| %>
   <li><%= message %></li>
  </ul>
  <% end %>
<% end %>


<%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |b| %>

  Title<p>
  <%= b.text_field :title %><p>

  Opinion<p>
  <%= b.text_area :body %>
<p>

  <%= b.submit 'Update Book' %>
<% end %>

<%= link_to "Show", book_path %>
|
<%= link_to "Back", books_path %>
</div>
</div>
</div>