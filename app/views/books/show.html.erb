<h1>Book detail</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col"><%= image_tag @books.get_image %><p></th>
      <th scope="col"><%= @books.title %></th>
      <th scope="col"><%= @books.body %></th>
        <th scope="col">  <% if @books.user == current_user %>
    <%= link_to "Edit", edit_book_path %>
  <% end %></th>
          <th scope="col">  <% if @books.user == current_user %>
    <%= link_to "Destroy", book_path(@books), method: :delete %>
  <% end %></th>
    </tr>
    <tr>
    <th scope="col"><%= @books.user.name %></th>
    </tr>


  </thead>
</table>

